<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
            <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/tailwind.css">
</head>
<body>
    {{>header}}
    <div class="container-width grid grid-cols-4">
        {{#each users}}
            <div class="user shadow-md grid grid-cols-4">
                <span>{{this.name}}</span>
                <span>{{this.email}}</span>
                <span>{{this.role}}</span>
                <span>{{this.password}}</span>
            </div>
        {{/each}}
    </div>
</body>
</html>